# Features

Product requirement and verification files for this repo. Process docs live in
the build-system repo pointed at by `.dori-build.json`.

Do not put patient names, record IDs, or clinical sample content in these
files. Describe product behavior only.

See `docs/reverse-engineering.md` in the process repo for as-built inventory.

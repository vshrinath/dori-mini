node_modules/
.env
.pnpm-store/

# The credentials store itself ships (macOS-only — Keychain/pbcopy). Its bulk importer
# does not: it defaults to a path in the author's own vault layout and is a personal
# convenience, not part of the feature.
import-credentials.mjs

# Personal credential inbox, if one is ever created in-tree.
credentials-inbox.md
